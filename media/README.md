# Media

## Live demo

[`whatsthescore-demo.html`](whatsthescore-demo.html) is a self-contained interactive walkthrough of the app (HTML + JS, no external assets). Served via GitHub Pages at:

> https://vladpetrariu.github.io/WhatsTheScore-Preview/media/whatsthescore-demo.html

The README links to that URL.

## If you want to add a video later

GitHub READMEs auto-embed `.mp4` files referenced as raw URLs:

```markdown
https://github.com/VladPetrariu/WhatsTheScore-Preview/raw/main/media/loop.mp4
```

Tips:
- Keep `loop.mp4` under ~10 MB so it loads fast on the README.
- For higher-quality assets (App Store hero video, large MP4s), upload to a release or use Git LFS rather than committing to `main`.
